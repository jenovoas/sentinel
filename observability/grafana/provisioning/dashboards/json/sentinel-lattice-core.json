{
  "dashboard": {
    "id": null,
    "uid": "sentinel-lattice-core",
    "title": "Sentinel Lattice Core",
    "tags": ["sentinel","lattice","core"],
    "timezone": "browser",
    "schemaVersion": 29,
    "version": 0,
    "panels": [
      {
        "type": "timeseries",
        "title": "Lattice Total Energy",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_lattice_total_energy", "legendFormat": "total_energy", "refId": "A" }
        ],
        "gridPos": { "x": 0, "y": 0, "w": 24, "h": 9 }
      },
      {
        "type": "timeseries",
        "title": "Lattice Entropy (S60)",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_lattice_entropy_s60", "legendFormat": "entropy", "refId": "B" }
        ],
        "gridPos": { "x": 0, "y": 9, "w": 24, "h": 9 }
      },
      {
        "type": "timeseries",
        "title": "Coherencia Lattice",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_lattice_coherence", "legendFormat": "coherence", "refId": "C" }
        ],
        "gridPos": { "x": 0, "y": 18, "w": 24, "h": 9 }
      },
      {
        "type": "timeseries",
        "title": "Calor / Lattice Heat",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_lattice_heat", "legendFormat": "heat", "refId": "D" }
        ],
        "gridPos": { "x": 24, "y": 0, "w": 24, "h": 9 }
      },
      {
        "type": "timeseries",
        "title": "Dual Channel Amplitud",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_dual_channel_amplitude", "legendFormat": "amplitude", "refId": "E" }
        ],
        "gridPos": { "x": 24, "y": 9, "w": 24, "h": 9 }
      },
      {
        "type": "timeseries",
        "title": "Dual Channel Fase",
        "datasource": "Prometheus",
        "targets": [
          { "expr": "sentinel_dual_channel_phase", "legendFormat": "phase", "refId": "F" }
        ],
        "gridPos": { "x": 24, "y": 18, "w": 24, "h": 9 }
      }
    ]
  },
  "overwrite": true
}
