[package]
name = "sctl"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sysinfo = "0.30"
colored = "2.1"
anyhow = "1.0"
# For hugepage/system tuning via libc if needed later
libc = "0.2"
