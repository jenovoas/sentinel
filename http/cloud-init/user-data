#cloud-config
password: sentinel
chpasswd: { expire: False }
ssh_pwauth: True
hostname: sentinel-cortex

users:
  - name: debian
    sudo: ALL=(ALL) NOPASSWD:ALL
    lock_passwd: false
    shell: /bin/bash

runcmd:
  - systemctl start ssh
