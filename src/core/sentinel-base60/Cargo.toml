[package]
name = "sentinel-base60"
version = "0.1.0"
edition = "2021"
authors = ["Sentinel Team"]
description = "Pure Base-60 (Sexagesimal) mathematics library - Zero decimal contamination"
license = "MIT"

[dependencies]
# No external dependencies - pure Rust for maximum reliability and zero friction

[dev-dependencies]
# For property-based testing in the future
# proptest = "1.0"

[lib]
name = "sentinel_base60"
path = "src/lib.rs"

[[test]]
name = "arithmetic"
path = "tests/arithmetic.rs"

[[test]]
name = "contamination"
path = "tests/contamination.rs"
