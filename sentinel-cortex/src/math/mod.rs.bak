//! # S60 Math Module Consolidation
//! Unified via me60os_core/SPA (60^4)

pub mod harmonic_logic;

// Compatibility Bridge for Legacy S60 Imports
pub mod s60 {
    pub use me60os_core::spa::SPA as S60;
    // Note: SPA doesn't have a separate S60Error, it uses internal Result
}

pub mod s60_math {
    pub use me60os_core::spa_math::*;
}

pub use me60os_core::spa::SPA as S60;
pub use me60os_core::spa_math::SPAMath;
